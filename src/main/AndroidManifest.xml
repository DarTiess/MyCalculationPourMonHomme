<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mycalc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NzListActivity" />
        <activity android:name=".TransferFromNzActivity" />
        <activity android:name=".NzActivity" />
        <activity android:name=".DepositActivity" />
        <activity android:name=".DepositListActivity" />
        <activity android:name=".WeekActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ListActivity" />
        <activity android:name=".CalculatActivity" />
        <activity
            android:name=".StartupActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>